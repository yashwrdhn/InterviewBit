#include <conio.h>
#include<bits/stdc++.h>

using namespace std;


int main(){
		
	int n;
	cin>>n;
	vector<int> arr;

	for(int i=0; i<n;++i){
		cin>>x;
		arr.push_back(x);
	}

	auto itr = upper_bound(arr.begin()+)

	getch();
	return 0;
}