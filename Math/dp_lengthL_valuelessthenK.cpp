//pending

#include <conio.h>
#include<bits/stdc++.h>

using namespace std;

int find_subsets(vector<int> arr,int b,int c,int &k, int &len,int &count,int &sum){
	if(sum + arr[k] < c && len < b){
		return 
	}
}

int main(){
	

	vector<int> arr = {0,1,2,3,6,7};
	int b = 3;
	int c = 7;
	int count = 0;
	int sum = 0;
	cout<< find_subsets(arr,b,c,k,len,count,sum);
	getch();
	return 0;
}